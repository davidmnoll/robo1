version: "3.9"

services:
  ros-core:
    environment:
      # - API_BASE_URL=${CLOUD_RUN_API_URL:?Set CLOUD_RUN_API_URL (App Engine base e.g. https://robot-gateway-.../api)}
      # - ROS_PUSH_KEY=${CLOUD_RUN_LOBBY_KEY:?Set CLOUD_RUN_LOBBY_KEY to lobby access key}
      - API_BASE_URL=https://34.42.43.54.sslip.io/api
      - ROS_PUSH_KEY=0Qwnn2YcqcHpnB9YNcBcbw

  sim:
    environment:
      - ROS_BRIDGE_HOST=ros-core
      - ROS_BRIDGE_PORT=9090
